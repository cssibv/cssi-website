# ───────── Secrete ─────────
secrets.php
.env
.env.local
*.env

# ───────── Backups & temp ─────────
*.bak
*.backup
*.old
*.orig
*.save
*~

# ───────── Uploads (datele de utilizator NU se versionează) ─────────
# Dar PĂSTRĂM .htaccess pentru blocare execuție PHP în uploads
admin/uploads/*
!admin/uploads/.htaccess
uploads/*
!uploads/.htaccess

# ───────── Local dev / IDE ─────────
.vscode/
.idea/
.DS_Store
Thumbs.db

# ───────── Excel lock files ─────────
.~lock.*
~$*

# ───────── Claude / dev tools ─────────
.claude/
*.bak-*

# ───────── Materiale interne de marketing (nu se urcă pe site) ─────────
daily-briefs/
google-ads-plan/
weekly-strategy/
proiecte/

# coada de continut vine din repo-ul privat cssi-content (checkout in CI)
.publicare/
